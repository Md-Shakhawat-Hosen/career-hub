import React from 'react';
import { useLoaderData, useParams } from 'react-router-dom';
import JobDetails from '../FeaturedJobs/JobDetails/JobDetails';


const Statistics = () => {
    //  const jobs = useLoaderData();
    //  const id = useParams();
    //  console.log(jobs);
    //  console.log(id);
     
    return (
        <div>
            <h1>statistics</h1>
            
            
        </div>
    );
};

export default Statistics;